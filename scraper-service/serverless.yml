service: scraper-service

frameworkVersion: "3"

provider:
  name: aws
  runtime: nodejs18.x


functions:
  hello:
    handler: dist/app.handler
    events:
      - http: ANY /
      - http: ANY /{proxy+}